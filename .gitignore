node_modules
.env
.expo
.rork

# Cloudflare Functions
functions/node_modules
!functions/package.json

# Wrangler local dev (never commit — .dev.vars holds real Supabase keys)
functions/.dev.vars
functions/.wrangler
